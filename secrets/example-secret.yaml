# example-secret.yaml

## Usage: Run ../encrypt_files_age.sh with secrets in this dir and the encrypted copies will be appended to the ../encrypted folder
## DO NOT COMMIT THIS DIRECTORY!

# Example secret
config:
  # Example values
  database:
    redis:
      host: redis
      username: user
      password: password
    postgres:
      host: postgres
      username: pguser
      password: pgpassword
